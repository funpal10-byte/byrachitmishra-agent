# Both providers are listed so you can switch between them without touching
# this file. Only the one selected by LLM_PROVIDER is actually called.
anthropic>=0.40
google-genai>=1.0
jinja2>=3.1
playwright>=1.44
pyyaml>=6.0
requests>=2.31
